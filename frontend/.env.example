# Base URL the React app uses to reach the Django API.
# Local dev:
REACT_APP_API_URL=http://127.0.0.1:8000/api
#
# Production (set this in Vercel → Project → Settings → Environment Variables):
# REACT_APP_API_URL=https://skillsync-backend.onrender.com/api
